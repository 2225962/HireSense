{
  "name": "hiresense-ai",
  "version": "1.0.0",
  "description": "AI Resume Screening Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
